namespace FastMath.Core.Models
{
    /// <summary>
    /// Specifies the degrees of freedom mode for a statistical calculation.
    /// </summary>
    /// <remarks>Use this enumeration to indicate whether the calculation should use a fixed or randomized
    /// degrees of freedom approach. The choice of mode can affect the statistical properties of the result.</remarks>
    public enum OperandDof {fix, randomized}
}
